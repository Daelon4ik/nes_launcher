/// Ищет описание и обложку игры по названию ROM во внешнем источнике метаданных.
pub async fn fetch_metadata(rom_title: &str) -> Result<(), String> {
    todo!("запрос к источнику метаданных (см. docs/data-model.md: Settings.metadata_source)")
}
